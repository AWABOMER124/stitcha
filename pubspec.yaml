name: wassalk_app
description: "A multi-vendor delivery application for Sudan."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.3

  # State Management
  flutter_riverpod: ^2.4.9
  
  # Routing
  go_router: ^13.1.0

  # Networking
  dio: ^5.11.0
  connectivity_plus: ^5.0.2
  
  # Storage & Security
  flutter_secure_storage: ^9.0.0
  shared_preferences: ^2.2.3
  
  # Images & Performance
  cached_network_image: ^3.3.1
  
  # Real-time & Notifications
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  flutter_local_notifications: ^18.0.1

  # Maps
  google_maps_flutter: ^2.9.0

  # Debugging & Logging
  logger: ^2.0.2
  
  # Data Classes & JSON
  freezed_annotation: ^3.1.0
  json_annotation: ^4.12.0
  
  # Theming & Fonts
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.16.0
  freezed: ^4.0.1
  json_serializable: 6.14.1

flutter:
  uses-material-design: true
  generate: true
  # assets:
  #   - assets/images/
  #   - assets/icons/
