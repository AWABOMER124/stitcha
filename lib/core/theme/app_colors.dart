import 'package:flutter/material.dart';

/// WASLA semantic design palette.
class AppColors {
  // --- Brand (Ruby & Platinum) ---
  static const primary = Color(0xFF13C4A3);
  static const primaryDark = Color(0xFF07111F);
  static const primaryLight = Color(0xFF6EE7D2);
  static const accent = Color(0xFF3B82F6);
  static const secondary = Color(0xFF3B82F6);

  // --- Surfaces ---
  static const background = Color(0xFFF6F8FB);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceElevated = Color(0xFFF2F2F7);
  static const surfaceDark = Color(0xFF07111F);

  // --- Neutral ---
  static const greyLight = Color(0xFFF1F1F6);
  static const greyMedium = Color(0xFFC7C7CC);
  static const divider = Color(0xFFE2E8F0);

  // --- Text (Tiered) ---
  static const textPrimary = Color(0xFF07111F);
  static const textSecondary = Color(0xFF64748B);
  static const textHint = Color(0xFF8E8E93);
  static const textWhite = Colors.white;

  // --- Status & Feedback ---
  static const success = Color(0xFF16A34A);
  static const warning = Color(0xFFD97706);
  static const error = Color(0xFFDC2626);
  static const info = Color(0xFF2563EB);
}
